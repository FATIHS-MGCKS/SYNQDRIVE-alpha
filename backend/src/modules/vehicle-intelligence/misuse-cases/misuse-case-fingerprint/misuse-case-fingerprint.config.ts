/** Fingerprint contract version — bump triggers supersede of prior cases (P48). */
export const MISUSE_CASE_FINGERPRINT_VERSION = 'misuse-fingerprint-v1';

/** Temporal bucket for evidence without stable sourceId (15 minutes). */
export const MISUSE_CASE_TEMPORAL_BUCKET_MS = 15 * 60 * 1000;
