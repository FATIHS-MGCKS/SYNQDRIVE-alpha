/** Maximum canonical stored text length (WhatsApp Cloud API text limit). */
export const CANONICAL_MESSAGE_TEXT_MAX_LENGTH = 4096;

/** Inbox preview length — matches native WhatsApp/SMS preview convention. */
export const CANONICAL_MESSAGE_PREVIEW_MAX_LENGTH = 120;

/** Content idempotency key version prefix. */
export const CANONICAL_CONTENT_IDEMPOTENCY_VERSION = 'cmc1';
