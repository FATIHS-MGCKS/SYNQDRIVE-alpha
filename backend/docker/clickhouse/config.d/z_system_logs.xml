<?xml version="1.0"?>
<!--
  System *_log tables: disable noisy defaults, short TTL on the few we keep.
  z_ prefix ensures this merges after package defaults (alphabetical config.d order).
-->
<clickhouse>
    <asynchronous_metric_log remove="1"/>
    <text_log remove="1"/>
    <trace_log remove="1"/>
    <processors_profile_log remove="1"/>
    <query_thread_log remove="1"/>
    <query_views_log remove="1"/>
    <query_metric_log remove="1"/>
    <session_log remove="1"/>
    <!-- metric_log merge caused OOM on 2026-08-21 (see clickhouse-production-recovery-2026-08 audit) -->
    <metric_log remove="1"/>

    <query_log replace="1">
        <database>system</database>
        <table>query_log</table>
        <engine>ENGINE = MergeTree PARTITION BY (event_date) ORDER BY (event_time) TTL event_date + INTERVAL 7 DAY DELETE</engine>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    </query_log>

    <part_log>
        <ttl>event_date + INTERVAL 7 DAY DELETE</ttl>
    </part_log>
</clickhouse>
