/** Shared L1 surface tokens for Booking Detail tab panels. */
export const bd = {
  /** Premium solid card shell — use for all tab section containers. */
  card: 'surface-premium rounded-lg p-4 overflow-hidden',
} as const;
