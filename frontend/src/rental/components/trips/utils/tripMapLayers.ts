import type { TripMapLayerState } from '../trips-map.types';

export const DEFAULT_TRIP_MAP_LAYERS: TripMapLayerState = {
  showSpeed: true,
  showStops: true,
  showDrivingEvents: true,
  showAbuseEvents: true,
  showMatchedRoute: true,
};
