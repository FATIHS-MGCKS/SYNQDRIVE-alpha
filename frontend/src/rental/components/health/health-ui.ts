/** Health module surface tokens. */
import { rs } from '../../lib/rental-surface-ui';

export const healthUi = {
  moduleCard: rs.cardMd,
  detailPanel: rs.cardPadding,
  quickTile: 'surface-elevated rounded-xl overflow-hidden',
  statusRow: rs.panel,
} as const;
