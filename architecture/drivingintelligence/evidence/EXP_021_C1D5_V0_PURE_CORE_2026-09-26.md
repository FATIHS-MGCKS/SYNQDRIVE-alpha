# EXP-021 C1D.5 — DI V0 pure shadow core (S0/S1)

**Date:** 2026-09-26  
**Pilot context:** C1-MOBILE-PILOT-R1-001 (WOB L 7503) — golden tests use minimal derived fixtures only.  
**Gate:** `C1D5_GATE_READY_FOR_PREMERGE_REVIEW` (library only; no deploy).

## Scope delivered

- Governance: `DI-DEC-V0-SHADOW-PURE-CORE-001` (PROPOSED), change ledger, `CURRENT_STATE.md` row.
- Code: pure TypeScript core under `driving-intelligence/core/` with Jest tests (22) and forbidden-import static audit.

## Out of scope (explicit)

PostgreSQL/Prisma, BullMQ workers, Redis, DIMO GraphQL, feature flags, legacy mutation, customer APIs.

## L3 authority

Matches sealed C1D analysis: calendar-second support window; `(hav(P[t−1],P[t]) + hav(P[t],P[t+1])) / 2 × 3.6` km/h when all three labels are present, coordinates valid, and position states are FRESH (densified grid: `FIRST_OR_AFTER_ROW_GAP` only after ROW_ABSENT gaps, not sparse-DK index 0).

## Validation

`cd backend && npx jest --testPathPattern='driving-intelligence/core'`

## Evidence ID

- `DI-EVID-EXP021-C1D5-001` (this document + code path `driving-intelligence/core/`)
